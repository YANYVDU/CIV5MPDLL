# 开放边境绑定玩家(而非队伍)改造 —— 代码审查报告

- **审查对象**:CIV5MPDLL 当前工作区未提交改动(跨 12 文件)
- **审查时间**:2026-09-25
- **结论**:改造思路清晰、调用点迁移较完整,但存在 **1 个会导致开放边境交易无效化的三方不一致**,以及 **1 个存档兼容性坏档点**,另有多处需修正的边界问题。

---

## 一、【致命】授予侧与执行侧不一致 → 新签开放边境无法移动穿越

三处改动互相冲突:

| 位置 | 角色 | 行为 |
|---|---|---|
| `CvDealClasses.cpp:2624 / 2986 / 3691` | 授予侧 | 只写**玩家级**,不再写队伍级 |
| `CvUnit.cpp:3568`(`canEnterTerritory`) | 移动执行侧 | 仍只读**队伍级** |
| `CvUnit.cpp:4289-4302`(`canMoveInto` 新闸门) | 收紧 | 仅当队伍级放行时才收紧;队伍级为 false 时整闸门跳过 |

**后果**:玩家 A 与 B 签开放边境 → A 的玩家级[B]=true,但队伍级[B] 保持 false → B 的单位在 `canEnterTerritory` 的队伍级检查处失败 → **进不去 A 的领土**。新闸门只会"收紧"(队伍级放行但玩家级未给则拒),从不会"授予",因此开放边境交易实际上不产生移动效果。

### 修复方案(二选一)

- **方案 A —— 玩家级作为唯一权威(推荐,契合注释意图)**:把 `CvUnit.cpp:3568` 的放行条件改为玩家级,例如
  `GET_PLAYER(该地块拥有者).IsAllowsOpenBordersToPlayer(getOwner())`,
  授予才能真正生效,`canMoveInto` 闸门随之自洽。
- **方案 B —— 队伍级授予 + 玩家级收紧**:授予侧同步写队伍级(`SetAllowsOpenBordersToPlayer` 内部镜像给 `SetAllowsOpenBordersToTeam`),但撤销路径 `CvDealClasses.cpp:3691` 也须清理队伍级,否则 `true` 泄漏永不回收。

> 需向用户确认本意:(A) 玩家级开放边境应能真正授予他国单位穿越;还是 (B) 仅用于"防止同队其余玩家蹭开放边境"的收紧。

---

## 二、【坏档点】存档兼容性违反 CLAUDE.md 版本门控规则

`CvPlayer::Write`,`CvPlayer.cpp:31333-31340`:

1. **`bool bValid = m_bPlayerOBsValid || true;` 恒为 true(致命)**:
   旧档玩家 flag=false(靠队伍级回退)。一旦保存,其**全 false** 的玩家级数组被当作权威固化;读档后这些玩家在没有重新议约的情况下,**永久丢失原本有效的队伍级开放边境**。
   **应改为 `bool bValid = m_bPlayerOBsValid;`。**

2. **未用 MOD_SERIALIZE 版本门控**:
   CLAUDE.md 明确要求新增序列化成员使用 `#if defined(MOD_SERIALIZE)` + 版本宏门控(`if (uiDllSaveVersion >= ...)`)。
   当前是手写"结尾追加 + 读越界判 EOF 为 false",依赖 `FDataStream` 具体行为、未经验证,越界读可能 assert 或读到脏数据。
   **建议**:`MOD_DLL_VERSION_NUMBER` 由 164 → 165,对该块做版本门控读。

3. **reset 数组初始化范围不匹配**:
   `CvPlayer.cpp:1437-1441` 用 `iI < MAX_PLAYERS`,但成员数组为 `REALLY_MAX_PLAYERS`,新开档 `[MAX_PLAYERS, REALLY_MAX_PLAYERS)` 槽位未初始化(而读写按 REALLY_MAX_PLAYERS 全量)。
   **建议 memset 全数组(REALLY_MAX_PLAYERS)。**

---

## 三、其它问题

- **canMoveInto 闸门未排除 `MOVEFLAG_IGNORE_RIGHT_OF_PASSAGE`**:
  应忽略权利通过权的合法移动(AI 跨境侦察/探路等)可能被误拒。建议补充排除该 flag([`CvUnit.cpp:4289-4302`](CvGameCoreDLL_Expansion2/CvUnit.cpp#L4289-L4302))。
- **队伍合并/拆分不同步玩家级**:
  `CvTeam.cpp:528-544` 在成员变更时会传播队伍级开放边境,但同步不了玩家级数组 → 同队队员加入/退出后玩家级与队伍级脱节。若是预期收紧行为需确认。
- **残留 13 处 `IsAllowsOpenBordersToTeam` 读取**:已迁移主路径(旅游/谍报/AI)均更新,剩余多为队伍合并/函数定义,趋势一致,需注意勿漏。
- **`IsAllowsOpenBordersToPlayer` 对 `NO_PLAYER` 无防护**:有越界 assert 风险;当前调用点基本安全,建议在函数内判 `ePlayer >= 0`。

---

## 四、建议的修复次序

1. `Write` 改 `bValid = m_bPlayerOBsValid;`(修坏档)。
2. `canEnterTerritory:3568` 放行条件切到玩家级(让授予生效),并复核 `canMoveInto` 闸门自洽。
3. 序列化块改版本门控读(版本 164 → 165)。
4. reset 全数组 memset;闸门补充 `MOVEFLAG_IGNORE_RIGHT_OF_PASSAGE`。

> 注:CIV5MPDLL 源码注释须用英文;本文档为审查交付物,故用中文。

---

# 二审结论(2026-09-25,改动更新后复查)

## ✅ 一审问题已修复确认

1. **授予与执行不一致 → 已修复**。根因修法采用"玩家级唯一权威":
   - `canEnterTerritory` 签名新增 `PlayerTypes ePlotOwnerPlayer`([CvUnit.h:207](CvGameCoreDLL_Expansion2/CvUnit.h#L207));
   - 放行条件改为玩家级优先([CvUnit.cpp:3567-3578](CvGameCoreDLL_Expansion2/CvUnit.cpp#L3567-L3578)):`GET_PLAYER(ePlotOwnerPlayer).IsAllowsOpenBordersToPlayer(getOwner())` 命中即 `return true`;
   - 一审那个"只收紧"的 `canMoveInto` 闸门已删除,统一由 `canEnterTerritory` 传 `plot.getOwner()` 处理([CvUnit.cpp:4295](CvGameCoreDLL_Expansion2/CvUnit.cpp#L4295))。
   → 授予侧写玩家级,执行侧读玩家级,新签开放边境可正常穿越。✓
2. **`Write` 的 `|| true` 坏档 → 已修复**,改为直接写出 `m_bPlayerOBsValid`。
3. **reset 数组范围 → 已修复**,改 `REALLY_MAX_PLAYERS`。
4. **NO_PLAYER/越界防护 → 已补**,`IsAllowsOpenBordersToPlayer` 对 `ePlayer<0 || ePlayer>=MAX_PLAYERS` 返回 false。
5. **序列化改为 MOD_SERIALIZE 门控宏**形式(方向正确)。
6. **WorldBuilder 地图设置的队级 OB 已镜像到玩家级**(`CvWorldBuilderMapLoader.cpp:1158-1175`),避免玩家级唯一权威下出生即不一致。

整体质量较一审显著提升,核心数据流自洽。

## 🟡【已澄清 · 无需处理】版本门控用的是内测版本号 164

- `MOD_DLL_VERSION_NUMBER = 164`([CustomMods.h:36](CvGameCoreDLL_Expansion2/CustomMods.h#L36)),读侧门控为 `MOD_SERIALIZE_READ(164, ...)`,即 `if(uiDllSaveVersion >= 164)`。
- 严格来说:164 是**内测版本号**,此功能随 164 一起上线;内测期存档格式不保证向前兼容,旧档(163 及更早)不满足 `>=164` 会走 default,且内测坏了可接受。
- **结论**:按用户决定,不 bump、不改门控。若未来进入稳定版需与更早存档兼容,届时再统一 bump 并调整门控参数即可。

## ⚠️【中】canEnterTerritory 的 else 回退队级,可能绕过"收紧"意图

[CvUnit.cpp:3567-3578](CvGameCoreDLL_Expansion2/CvUnit.cpp#L3567-L3578)
```cpp
if(ePlotOwnerPlayer != NO_PLAYER && GET_PLAYER(ePlotOwnerPlayer).IsAllowsOpenBordersToPlayer(getOwner()))
    return true;              // 玩家级授予 → 放行
else {                        // 玩家级未命中(含同队另一玩家被拒绝)
    if(kTheirTeam.IsAllowsOpenBordersToTeam(eMyTeam))
        return true;          // ← 仍按队级放行
}
```
- 本功能意图之一是"防止同队另一玩家 B 蹭 A 与 C 的开放边境"。但这里玩家级未授予时 `else` 仍回退队级放行。
- 若 A 的队伍级恰好为 true(来源:`makeTeam` 队伍合并 `CvTeam.cpp:528-544`、或 WB 镜像),则 B 会被这支队级路径放行 → 收紧失效。
- **建议**:区分"玩家级系统已启用(新档)"与"旧档回退"。新档应在玩家级未授予时直接拒绝,仅旧档(玩家级 flag 无效)才回退队级。目前 IsAllowsOpenBordersToPlayer 在旧档已内含队级回退,else 再回退属重复且扩大口径。

## ⚠️【中】`makeTeam` 合并不同步玩家级(残缺口)

- WorldBuilder 的队级 OB 已镜像玩家级,但**队伍合并**路径 `CvTeam.cpp:528-544` 仍只写队级、不同步玩家级。
- 与上一条 B 叠加,合并产生的队级 OB 仍可经 else 放行,且该队各成员的玩家级数组为空,后续按玩家级查询(旅游/谍报/AI)与移动执行会不一致。
- **建议**:在 `makeTeam` 传播队级 OB 处同样镜像玩家级(可复用 WorldBuilder 的逐玩家循环写法)。

## 🟡【低】其它

- **DoAttrition**([CvUnit.cpp:8651](CvGameCoreDLL_Expansion2/CvUnit.cpp#L8651)):新用 `GET_PLAYER(pPlot->getOwner())`,`pPlot` 已在前面 `eOwnerTeam=pPlot->getTeam()` 解引用过,非空;无主地块 `eOwnerTeam==NO_TEAM` 被外层 `if` 挡住,不会进 `GET_PLAYER(NO_PLAYER)`。安全,但建议加 `getOwner()!=NO_PLAYER` 防御以防后续改到前面。
- **Write 的数组**用裸 `kStream << ArrayWrapperConst<bool>(REALLY_MAX_PLAYERS, ...)`,未包进 `MOD_SERIALIZE_WRITE_ARRAY` 宏。CIV5MPDLL 默认启用 MOD_SERIALIZE 无碍,但非 MOD_SERIALIZE 构建会"写多读少"不对称。建议统一用宏或显式 `#if defined(MOD_SERIALIZE)` 包裹。
- 越界防护用 `MAX_PLAYERS` 上界,数组为 `REALLY_MAX_PLAYERS`(`>=MAX_PLAYERS` 一律 false)。合法玩家索引均 <MAX_PLAYERS,行为正确。

## 二审修复清单(建议,按用户决定)

1. ~~【严重】bump 版本 164 → 165~~ —— **无需处理**:164 为内测版本,内测期不保证存档兼容。
2. 【中】canEnterTerritory else 分支按"玩家级系统是否启用"区分,避免新档被队级绕过(**若收紧同队蹭 OB 是本意,建议处理**)。
3. 【中】makeTeam 合并时镜像玩家级(WB 已镜像,合并路径补齐)。
4. 【低】DoAttrition 加 `getOwner()!=NO_PLAYER` 防御;Write 数组改用 MOD_SERIALIZE 宏包裹。

---

# 复审回应(2026-09-25,二审后逐条核对源码现状)

以下逐条对二审清单给出处理结论,并以当前源码为准核验。

## 1.【已澄清 · 无需处理】版本门控 164
维持 164、不改门控。内测版本号,本功能随 164 上线,旧档(≤163)不满足 `>=164` 走 default;内测期不保证存档向前兼容,坏档可接受。未来进稳定版需兼容更早存档时再统一 bump + 调门控参数。

## 2.【已采纳 · 已修复】canEnterTerritory else 回退队级被收紧
[CvUnit.cpp:3567-3578](CvGameCoreDLL_Expansion2/CvUnit.cpp#L3567-L3578):
```cpp
if(ePlotOwnerPlayer != NO_PLAYER && GET_PLAYER(ePlotOwnerPlayer).IsAllowsOpenBordersToPlayer(getOwner()))
    return true;
else if(ePlotOwnerPlayer == NO_PLAYER)
{
    if(kTheirTeam.IsAllowsOpenBordersToTeam(eMyTeam)) return true;  // 仅无法确定 owner 才回退队级
}
```
- 能确定 owner 且玩家级未授予(如同队另一玩家 B)→ 走到函数尾部**默认拒绝**,队级无法再绕过。
- 仅当 `ePlotOwnerPlayer == NO_PLAYER`(无法确定地块 owner)才回退队级,避免误拒。
- `IsAllowsOpenBordersToPlayer` 自身已对旧档内含队级回退,故新档收紧、旧档兜底均自洽。"防同队蹭 OB"意图达成。

## 3.【不采纳,有保留】makeTeam 合并镜像玩家级
**决定:不做镜像,维持玩家级收紧语义。理由:**
1. 若把"队伍合并产生的队级 OB"镜像成玩家级授予,会让同队**未签**的 B 自动获得队友的开边 —— 恰恰违反"只对签定双方生效"的本意。
2. 第 2 条的收紧已封死"队级绕过":当前对"owner 确定 + 玩家级未授予"是直接拒绝,合并遗留的队级 OB 无法再经队级路径放行他人。
3. 队伍合并本就是罕见操作,合并前各队员的玩家级开边按玩家保留、不扩散给新队友,符合玩家级权威预期。
- 若后续希望"合并时把双方已签玩家的玩家级开边归属保持/合并",可再议,但**不建议**把队级 OB 无条件镜像为玩家级授予。

## 4.【已采纳 · 已修复】低级项
- **DoAttrition**:已补 `pPlot->getOwner() != NO_PLAYER` 防御([CvUnit.cpp:8654](CvGameCoreDLL_Expansion2/CvUnit.cpp#L8654)),空/无主防护到位。
- **Write 数组**:已改用 `MOD_SERIALIZE_WRITE_CONSTARRAY`([CvPlayer.cpp:31337](CvGameCoreDLL_Expansion2/CvPlayer.cpp#L31337)),与 Read 端 `MOD_SERIALIZE_READ_ARRAY`([CvPlayer.cpp:30572](CvGameCoreDLL_Expansion2/CvPlayer.cpp#L30572))对称,规避非 MOD_SERIALIZE 构建"写多读少"不对称。

## 结语
全部二审遗留项已逐条落实或明确不采纳(含理由)。攻授侧、执行侧、序列化、边界防御均收敛到"玩家级唯一权威"的一致模型;唯一保留项(makeTeam 镜像)属有意的收紧语义,建议标注在代码注释中以便后续维护者理解。

---

# 复审回应(2026-09-25,二审后逐条处理)

二审整体质量高,一审问题均已确认修复;以下为对二审各点的处理结论。

## 版本门控 164 —— 维持,不改(按用户决定)
`MOD_DLL_VERSION_NUMBER` 保持 **164**(内测版)。二审已认可:内测期存档格式不保证向前兼容,旧档(≤163)走 default,坏档可接受;未来进入稳定版需兼容更早存档时再统一 bump 并调门控参数。

## 【采纳】二审问题1:canEnterTerritory else 队级绕过 → 已修复
`CvUnit.cpp` 把队级回退收紧为**仅当 `ePlotOwnerPlayer == NO_PLAYER`(无法确定地块 owner)时**才走队级;能确定 owner 且玩家级未授予时**直接拒绝**。
- 新档下 `IsAllowsOpenBordersToPlayer` 本身已含旧档 team 回退,因此该 else 无需再冗余回退;
- 由此封死"队伍合并/其它来源的队级 OB 绕过收紧"的核心漏洞 —— 同队未签者 B 无法再经队级路径蹭通 A 的开边。

## 【不采纳,有保留】二审问题2:makeTeam 合并镜像玩家级
**决定:不做镜像,维持玩家级收紧语义。理由:**
1. 若把"队伍合并产生的队级 OB"镜像成玩家级授予,会让同队**未签**的 B 自动获得 A 的开边 —— 恰恰违反"只对签定双方生效"的本意;
2. 问题1 的修复已封死"队级绕过"(新档队级不兜底),合并遗留的队级 OB 无法再绕过玩家级;
3. 队伍合并本就是罕见操作(游戏中途换队),而它导致的"玩家级与队级脱节"在新档玩家级权威下**符合预期**(合并前的个人开边关系,不应因合并而扩散给他人)。
- 若后续希望"合并时把双方已签玩家的玩家级开边归属保持/合并",可再议,但**不建议**把队级 OB 无条件镜像为玩家级。

## 【已修】二审问题3(低级)
- DoAttrition:`pPlot->getOwner()!=NO_PLAYER` 防御已补;
- Write 数组:改用 `MOD_SERIALIZE_WRITE_CONSTARRAY` 宏,与 Read 端 `MOD_SERIALIZE_READ_ARRAY` 对称,规避非 MOD_SERIALIZE 构建"写多读少"不对称。

## 状态
- 全量重编译通过,`CvGameCore_Expansion2.dll` 最新;diff 147+/34-,行尾完好。
- CIV5MPDLL 与 SP 两仓库改动均未提交。